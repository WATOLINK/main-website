{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm run dev -c -- --port 3001",
            "name": "Run Dev Server",
            "request": "launch",
            "type": "node-terminal"
        }
    ]
}