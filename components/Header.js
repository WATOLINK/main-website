import { StyledMainTitle } from './Headings'

const headers = []

export default function Header() {
    return (
        <div>
            <StyledMainTitle isBlue={true} myColor="red">Header</StyledMainTitle>
        </div>
    )
}
