import Head from 'next/head'
import Image from 'next/image'
import styled from 'styled-components'



export default function Projects() {
    return (
        <div>
            <Head>
                <title>WATOLINK</title>
                <meta
                    name="description"
                    content="Generated by create next app"
                />
            </Head>
            <TitleText></TitleText>
            <BodyText></BodyText>
        </div>
    )
}


const StyledTitleText = styled.div`
    color: ${({theme}) => theme.colors.blue};
    font: ${({theme}) => theme.fonts.heading};
    text-align: center;
    margin-top: 5%;
    margin-bottom: 5%;
`

const TitleText = ()=>{
    const titleText = "PROJECTS:"
    return(
        <StyledTitleText>{titleText}</StyledTitleText>
    )
}

const StyledBodyText = styled.div`
    color: ${({theme}) => theme.colors.black};
    font: ${({theme}) => theme.fonts.medium24bold};
    text-align: center;
`
const StyledSecondaryText = styled.div`
    color: ${({theme}) => theme.colors.blue};
    font: ${({theme}) => theme.fonts.medium24bold};
    text-align: center;
    margin-top: 2%;
    margin-bottom: 2%;
`

const BodyText = ()=>{

    const bodyText1 = "We are currently developing our project submission towards the NeuroTechX 2022 competition."
    const bodyText2 = "Showcase will be available after competition date."
    const bottomText = "Stay Tuned!"

    return(
        <div>
            <StyledBodyText>{bodyText1}</StyledBodyText>
            <StyledBodyText>{bodyText2}</StyledBodyText>
            <br></br>
            <StyledSecondaryText>{bottomText}</StyledSecondaryText>
        </div>
    );
};

//TO-DO, ADD Footer


